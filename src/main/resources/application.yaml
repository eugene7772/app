spring:
  application:
    name: app
app:
  jwt:
    secret: ${APP_JWT_SECRET:fgaqw342rvq3453dfgawer2342ef23f23f}
    ttl-minutes: ${APP_JWT_TTL_MINUTES:5}
  datasource:
    write:
      jdbc-url: jdbc:postgresql://localhost:15432/postgres
      username: postgres
      password: postgres
      driver-class-name: org.postgresql.Driver
    read:
      jdbc-url: jdbc:postgresql://localhost:25432/postgres
      username: postgres
      password: postgres
      driver-class-name: org.postgresql.Driver